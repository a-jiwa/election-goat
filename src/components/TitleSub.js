import React from 'react';

function TitleSub() {
    return <div className="title-sub">
        An updating calculation of the each party’s approval rating, accounting for each poll’s recency,
        sample size, methodology and house effects.
    </div>;
}

export default TitleSub;
